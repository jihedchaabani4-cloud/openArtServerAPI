export const GroqModerationService = {
    async moderateAndTranslate(prompt) {
        // Mock moderation and translation
        return {
            ok: true,
            translatedPrompt: prompt
        };
    }
};
